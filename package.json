{
  "name": "@paar-it-gmbh/odata-query",
  "version": "6.7.2",
  "author": "Sean Lynch <techniq35@gmail.com>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": "techniq/odata-query",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "dependencies": {
    "tslib": "^1.10.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "browserify": "^16.5.1",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "tsify": "^4.0.2",
    "typescript": "^3.5.2"
  },
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -p tsc -p tsconfig.json",
    "test": "jest -c jest.config.json",
    "test-watch": "jest -c jest.config.json --watchAll",
    "preversion": "npm run build"
  }
}
